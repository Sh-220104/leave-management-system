<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Suppress most noise from Spring / Hibernate during unit tests -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Keep test output readable – only WARN and above from Spring internals -->
    <logger name="org.springframework"         level="WARN"/>
    <logger name="org.hibernate"               level="WARN"/>
    <logger name="com.zaxxer.hikari"           level="WARN"/>
    <logger name="org.testng"                  level="INFO"/>

    <!-- Our own code at DEBUG during tests -->
    <logger name="com.epam.elms"              level="DEBUG"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
